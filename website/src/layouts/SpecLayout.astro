---
import Layout from "./Layout.astro";
const { frontmatter } = Astro.props;
---

<Layout title={`${frontmatter.title} — ponens spec`}>
  <article class="prose">
    <p class="spec-back"><a href="/spec">← All specifications</a></p>
    <slot />
  </article>
</Layout>
