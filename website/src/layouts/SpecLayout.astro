---
import DocsLayout from "./DocsLayout.astro";
const { frontmatter } = Astro.props;
---

<DocsLayout title={`${frontmatter.title} — ponens spec`}>
  <article class="prose">
    <p class="spec-back"><a href="/spec">← All specifications</a></p>
    <slot />
  </article>
</DocsLayout>
